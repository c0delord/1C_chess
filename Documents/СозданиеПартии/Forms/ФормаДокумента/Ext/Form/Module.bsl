&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Ключ.Пустая() Тогда
		Запрос = Новый Запрос;
		Запрос.Текст = "ВЫБРАТЬ
		|	Клетки.Ссылка КАК Клетка,
		|	Фигуры.Ссылка КАК Фигура
		|ИЗ
		|	Справочник.Клетки КАК Клетки
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Фигуры КАК Фигуры
		|		ПО Клетки.Ссылка = Фигуры.КлеткаПоУмолчанию";
		Выборка = Запрос.Выполнить().Выгрузить();
		Объект.Доска.Загрузить(Выборка);	
	КонецЕсли;
КонецПроцедуры
